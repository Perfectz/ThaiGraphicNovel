export type Enemy = {
  id: string;
  name: string;
  maxConfidence: number;
  openingLine: string;
};

export const suThaiBasicsTrial: Enemy = {
  id: 'su-thai-basics-trial',
  name: 'Su',
  maxConfidence: 100,
  openingLine: 'Su raises her hand and turns the lobby into a safe Thai Basics Trial.',
};
